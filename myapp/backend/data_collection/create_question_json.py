def create_question_json(text_filepath):
    #parse_input(filepath) → vocab_words[], sentences[]
    vocab_words = 
    sentences = 
    #load_definitions() → dict
    #generate_vocab_questions(vocab_words) → list of question dicts
    #generate_sentence_questions(sentences, vocab_words) → list of question dicts
    #generate_fib_questions(vocab_words, sentences) → list of question dicts
    #write_json(all_questions, output_path)
    